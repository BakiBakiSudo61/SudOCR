# 環境変数の設定例
# 本番環境では.envファイルを作成してこれらの値を設定してください

# CORS設定（カンマ区切りで複数指定可能）
ALLOWED_ORIGINS=https://sudocr.bakix2.jp,http://localhost:3000,http://127.0.0.1:3000

# アップロード制限（MB単位）
MAX_FILE_SIZE_MB=10

# ログレベル（DEBUG, INFO, WARNING, ERROR, CRITICAL）
LOG_LEVEL=INFO

# デバッグモード（開発環境でのみTrueに設定）
DEBUG=False

# OCR設定
OCR_LANGUAGE=japan
OCR_VERSION=PP-OCRv5

# サーバー設定
HOST=0.0.0.0
PORT=8000

# セキュリティ設定
TRUSTED_HOSTS=sudocr.bakix2.jp,localhost,127.0.0.1
